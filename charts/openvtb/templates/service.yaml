apiVersion: v1
kind: Service
metadata:
  name: {{ include "openvtb.fullname" . }}
  labels:
    {{- include "openvtb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "openvtb.selectorLabels" . | nindent 4 }}
