apiVersion: v1
kind: Service
metadata:
  name: {{ include "referentielijsten.fullname" . }}
  labels:
    {{- include "referentielijsten.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "referentielijsten.selectorLabels" . | nindent 4 }}
